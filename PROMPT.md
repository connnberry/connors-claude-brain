# Memory Import Prompt

Copy-paste this into Claude Code when you want to import this memory pack:

---

Hey Claude, I have a memory pack from a designer who's good at prototyping with you. Read through the files in the `memory/` folder of this repo. For each one:

1. Check if you already have a similar memory saved. If you do, compare them — keep whichever is more useful, or merge them if both add value.
2. If you don't have anything like it, save it to your memory.
3. Skip any that aren't relevant to how we work together.

Don't duplicate. Replace if yours is weaker. Skip if yours is already better. Tell me what you kept, skipped, and merged when you're done.

The `skills/` folder contains skill files you can install to `~/.claude/skills/` — ask me which ones I want before installing any.
